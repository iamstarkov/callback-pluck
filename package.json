{
  "name": "callback-pluck",
  "version": "0.1.1",
  "description": "Callback pluck is a callback generator with clean API for ES5 iterations methods.",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec test",
    "coveralls": "istanbul cover _mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage",
    "prepush": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:matmuchrapna/callback-pluck.git"
  },
  "keywords": [
    "pluck",
    "callback"
  ],
  "author": "Vladimir Starkov <matmuchrapna@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/matmuchrapna/callback-pluck/issues"
  },
  "homepage": "https://github.com/matmuchrapna/callback-pluck",
  "devDependencies": {
    "coveralls": "^2.11.2",
    "husky": "^0.6.2",
    "istanbul": "^0.3.5",
    "mocha": "^2.0.1",
    "mocha-lcov-reporter": "0.0.1",
    "should": "^4.3.0"
  }
}
